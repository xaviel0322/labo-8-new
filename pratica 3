# 1. Instalar docker-ce y docker-compose-plugin
dnf install docker-ce-cli docker-compose-plugin

# 2. Crear el directorio de WordPress
mkdir WordPress

services:
  db:
    image: mysql:5.7
    container_name: wordpress_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wordpress_user
      MYSQL_PASSWORD: wordpress_password
    volumes:
      - db_data:/var/lib/mysql

  wordpress:
    image: wordpress:latest
    container_name: wordpress_app
    restart: always
    depends_on:
      - db
    ports:
      - "8080:80"
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_USER: wordpress_user
      WORDPRESS_DB_PASSWORD: wordpress_password

volumes:
  db_data:

4. usaremos el siguiente el comando para ejecutar los servicios por separado

docker compose up - d

5. entramos al sitio web y configuramos 

127.0.0.1:8080

6. creamos nuestra pagina:

practica8so3
xaviel
docecarateresdelargo123
20221881@itla.edu.do
